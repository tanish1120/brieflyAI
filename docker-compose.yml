services:
  postgres:
    image: postgres:15
    container_name: brieflyai-postgres
    environment:
      POSTGRES_DB: brieflyai
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: